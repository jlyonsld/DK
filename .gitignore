# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node (in case we add a build step later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Local env (future-proofing; we don't have any today)
.env
.env.local
.env.*.local
*.env

# Secrets / local-only config — keep real secret values out of git.
# Convention: commit a redacted file, keep the real one as *.local.* (ignored).
*.local.gs
*-local.gs
*.secret
*.secret.*
secrets.*
config.local.*

# Supabase CLI local artifacts
.branches/
.temp/
supabase/.temp/
